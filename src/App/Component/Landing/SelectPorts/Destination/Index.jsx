import React, { Component } from 'react';

class Destination extends Component { // eslint-disable-line
    render() {
        return (
            <div>select destination port</div>
        );
    }
}

export default Destination;
