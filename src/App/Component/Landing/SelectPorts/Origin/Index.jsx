import React, { Component } from 'react';

class Origin extends Component { // eslint-disable-line
    render() {
        return (
            <div>select origin port</div>
        );
    }
}

export default Origin;
